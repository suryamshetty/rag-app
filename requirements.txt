# Streamlit (already installed, can use >= to avoid reinstallation)
streamlit>=1.26.0

# LangChain packages
langchain>=0.2.17
langchain-core>=0.2.43
langchain-community>=0.2.19
langchain-openai>=0.1.25
langchain-google-genai>=1.0.10

# Pydantic (needed for LangChain)
pydantic>=2.7.4
# Sentence transformers for embeddings
sentence-transformers>=2.2.2

# Google Gemini SDK
google-generativeai>=0.3.1

# Utilities (avoid forcing reinstall of preinstalled packages)
# Rich and Markdown packages can be optional if not heavily customized
# Optional utilities (only if strictly needed)
# rich>=14.2.0
# markdown-it-py>=4.0.0
# mdurl>=0.1.2
# pygments>=2.19.2